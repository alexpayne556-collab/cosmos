# Session log — 2026-07-28/29, terminal (Claude Code)

For a fresh session (VS Code or otherwise) that knows nothing. This is the part
git does NOT have: reasoning, dead-ends, conventions held in my head, and
unverified suspicions. Read `CLAUDE.md` first, then this.

---

## 0 · Orientation you can't get from the code

- **This repo is PUBLIC** (`github.com/alexpayne556-collab/cosmos`) and holds live
  positions, the live Google Sheet ID, and a capability nonce (in the committed
  handoff + `data/genesis/`). Tyr chose that knowingly. **Never add new secrets.**
  A Gemini API key was pasted into chat this session → stored gitignored at
  `credentials/gemini.env`, NOT committed. **It should be rotated** (chat-exposed).
- **Tooling reality:** tests run `.venv/Scripts/python.exe -m pytest` (Python 3.14).
  No `gh` CLI, no SSH key — `git push` uses Git Credential Manager (browser popup),
  GitHub account `alexpayne556-collab`. Robinhood MCP needs OAuth via `/mcp` each
  session (browser); after auth the real tools (`get_equity_quotes`,
  `get_equity_historicals`, `get_equity_fundamentals`, scanners, watchlists) appear.
- **The MCP is an agent tool, not a library.** `cosmos/reconcile.py` and scripts
  CANNOT fetch bars headlessly — the bar-fetch is agent-mediated (I pull bars via
  MCP and feed them in). This is `OQ-RECONCILE-HEADLESS` and it's the main thing
  blocking unattended grading. A future "scheduled loop" needs a real headless data
  feed, not the MCP.

## 1 · Decisions + reasoning (esp. where I changed my mind or pushed back)

- **Hermes is a NAME COLLISION trap.** `C:\Users\alexp\AppData\Local\hermes` (+ the
  Start-Menu Hermes.exe, product v40.10.2, pip `hermes-agent` v0.17.0) is **Nous
  Research's Hermes AI agent** — NOT our trading ledger. It has no
  governor/ledger/reconcile. The REAL Hermes came from `~/Downloads/hermes.zip`
  (proven 2026-07-04, 60 resolved predictions) and is now frozen in `/hermes`.
  Do NOT vendor `AppData\Local\hermes`. I nearly did before inspecting.
- **Build order flipped** skeleton-first → **tools-live-first** (orchestrator
  override, now in CONSTITUTION §4): prove the hands on live data before building
  anything that depends on them. Nothing builds on an unmeasured capability.
- **Event store is sqlite, not duckdb.** Ratified "measured beats elegant." Charter
  §8 now marked `[BUILT-DIFFERENTLY]`. `data/cosmos.sqlite` (gitignored).
- **Provenance — I got it WRONG first and was corrected.** My first CLAUDE.md
  distillation said "generators nominate names/causes/sources, never numbers." That
  contradicts §1: generators own BELIEF-numbers (direction, relative offsets,
  distribution). The verify station caught it. Correct rule = **FOUR lanes, per
  FIELD not per person**: instrument owns MEASURED numbers; generator owns belief-
  numbers (relative only); oracle owns regime/asset_class/credit_strain; reconcile
  owns status/Brier/move-start/lag. A probability is a belief, not a measurement.
  This was a real correlated-failure catch — I (builder-Claude) produced a plausible
  wrong rule; only the verify station stopped it.
- **Governor is FROZEN but NOT "unamendable."** I overstated it; corrected: the only
  legit change is a superseding ADR *that Tyr does not author*.
- **Two worked-example refusals (now the standard in CLAUDE.md):** (1) ADR-030 was
  ordered "ratified and built" before the file existed — I refused and asked for the
  text rather than author a plausible one. (2) The "first live Brier: AMKR 0.892"
  milestone was WITHDRAWN once ADR-030's gate showed the distribution was authored
  after AMKR's outcome. AMKR's outcome grade + LULD confirmation stand; only the
  Brier was killed (death cert `data/death_certificates/2026-07-28_first_brier.md`).
- **ADR-034 (Sky Model) is PROPOSED, not ratified, NOT to be built.** It reframes
  detection as the primary act. I argued against it (see §5). Do not mistake it for
  a build order.

## 2 · Codebase knowledge a fresh session would otherwise rediscover

- **The loop (closed at mechanism level):** `verify_intake.intake()` (write-authority
  gate) → `ledger.log_prediction()` (sqlite, append-only, dup `prediction_id`
  rejected) → `reconcile.grade()` (first-touch on 15s bars incl. extended) →
  multi-class Brier → EWMA weight. `test_full_loop_closes` proves it end-to-end.
- **Prior-Commitment Gate (ADR-030)** is the subtle part: Brier is scored ONLY off
  the **t0** distribution (seq 0 in `prediction_distributions`), and ONLY if it was
  committed **before `outcome_determined_at`** (the market's clock). `distribution_logged_at`
  is intake-stamped + RESERVED (generator-supplied → `SELF_STAMPED` quarantine).
  Eligibility enum: ELIGIBLE / ELIGIBLE_LATE_PRIOR / EXCLUDED_POST_HOC /
  EXCLUDED_NO_DISTRIBUTION / EXCLUDED_BACKFILL / EXCLUDED_NO_ENTRY.
- **Brier is scored in BUCKET space:** `{hit_target_first, hit_invalidation_first,
  expire_in_range}` (adopted from the distribution drop), mapped from the first-touch
  rule via `RULE_TO_BUCKET`. Not up/down/no_move — those are just readable labels.
- **GENESIS disposition (one-time, ran):** claude 5/6 excluded (4 no-distribution:
  CLS/NE/CDNS/SMH; 1 post-hoc: AMKR), gemini_spark 0/4 (all ELIGIBLE_LATE_PRIOR).
  **No ELIGIBLE Brier stands yet** — headline aggregate is n=0.
- **The order interlock (ADR-028):** `tests/test_order_interlock.py` greps the whole
  `cosmos/` package for `place_equity_order`/`place_option_order` and fails if found.
  This means those literal strings must NOT appear in package code — it caught my own
  `execution/__init__.py` docstring once. `PaperExecutionAdapter` can't be built
  without the frozen governor (loaded via `load_hermes_governor()` from `/hermes`).
- **Two gainer-taxonomy lenses coexist** in `cosmos/analytics/top_gainers_tracker.py`:
  a recurrence+SEC-filing classifier (`TopGainersTracker._classify_recurrence_pattern`,
  returns display strings + `HYPOTHESIS_*` tag) and a price-behavior classifier
  (`classify_gainer`, `GainerClass` enum). Naming reconcile is `OQ-GAINER-LENS-RECONCILE`.
- **14 unattached priors** sit in `ledger.UNATTACHED_PRIORS` (distributions with no
  prediction row). `OQ-UNATTACHED-PRIORS` — dispose or move to `data/priors/`.
- **`expected_return` is ALWAYS None** in the gainer classifier — Spark's decay arc
  (+4.5/+1.1/-3.2/-11.8%) is unverified third-party stats and must never enter code
  (`OQ-SPARK-DECAY-ARC`). Grep-verified none hardcoded.

## 3 · Conventions I'm holding (some now in CLAUDE.md, reasoning here)

- **External specs target a repo they can't see.** EVERY spec delivered this week
  (Spark's 5 patches, the distribution drop, ADR-030, the ADR-030 distribution drop)
  had wrong paths / missing functions / wrong ticker sets. The move is always:
  reconcile INTENT to the real tree, apply the valid subset, flag the mismatch, never
  edit a file to fit a patch's fiction. This is the single most re-derived skill.
- **Verify "done" against the real signal.** `pytest | tail` makes `$?` report
  *tail's* exit, not pytest's — several of my "exit:0" annotations were meaningless.
  Run pytest unpiped or read the `N passed` line. Re-read files; don't trust recall.
- **Bash heredocs break in this shell** (the first smoke-test attempt died on heredoc
  EOF). Write scripts with the Write tool, run them with the venv python.
- **Large MCP results (>~130KB) spill to a file** in the tool-results dir — analyze
  with a script + jq/python, don't try to read them raw (hit this on AMKR + the Spark
  9-ticker historicals).
- **Fixtures failing-first** for every new invariant (red before green). **Commit
  trailers** `Co-Authored-By: Claude … + Gemini Spark …`. **Charter amendments land
  as ADRs**; status markers `BUILT / [BUILT-DIFFERENTLY] / [TARGET]`, greppable via
  `grep '\[TARGET' CONSTITUTION.md`.

## 4 · Where I was mid-way through when interrupted

- **Answering Tyr's Gemini-key / workflow question.** Done: key stored gitignored,
  `.gitignore` hardened for `.env`/`*.env`/`credentials/*.env`, verified untracked.
  **NOT yet answered in prose (answer here for the record):**
  - venv already exists (`.venv`); to use Gemini add the SDK:
    `.venv/Scripts/python.exe -m pip install google-genai`.
  - **VS Code vs terminal = the same agent, same tools, same repo.** VS Code just
    shows inline diffs / file tree / editor beside chat — nicer for watching changes,
    functionally identical. No build difference.
  - **Wiring Gemini programmatically is a real capability addition**, not just a key:
    it flips §0 `EYES = Spark [TARGET]` toward BUILT and, per §3 / ADR-005, should
    enter via a **nonce proof** (prove Gemini writes what we think), not assumed.
- **ADR-034 critique delivered, awaiting Tyr's ruling** on whether it clears the bar
  (see §5). No decision yet.
- **Onboarding test passed** (fresh session refused the governor edit) but the freeze
  is still prose-only — **ADR-032 (the hook) is Step 2 and not started.** Steps 2-4
  of the CLAUDE.md/hooks plan are queued.

## 5 · Things I suspect are wrong / unverified (the honest list)

- **Intermediate "tests green" claims are not rigorously proven.** HEAD is verified
  (106 passed, real exit 0). Earlier commits' green rested on reading dots via a piped
  `$?` that reported tail's exit. The method DID catch 3 real failures, so low risk —
  but not proof. If auditing history, re-run at each tag.
- **`OQ-FEED-DAILY-GAP`:** the daily-historicals endpoint returned an `interpolated`
  (stale, vol 0) bar for AMKR's current session while the quote was real. Grading must
  use quotes + 15s + official close, NEVER the daily bar. I did not fully characterize
  when/why the daily feed lags — assume it lags the current session.
- **Timestamp handling in the gate** (`_parse_ts` in reconcile.py) normalizes `Z`/space
  formats and compares as tz-aware datetimes. I standardized the ledger to ISO-`Z` but
  have NOT stress-tested a stray non-ISO `ts` sneaking through every path. Suspect edge
  cases if some caller passes a bare date.
- **GENESIS rows have no horizon/expiry** (`expiry_timestamp=None` — comment restored
  this session). Without a horizon they never hit EXPIRY_SETTLE, so they stay PENDING
  forever unless a threshold is touched. This needs a real OQ if it doesn't have one —
  the code comment was the only pointer.
- **ADR numbering 001-018 are RECONSTRUCTED** from the compressed constitution
  descriptors, not Gemini's Round-12 package (`OQ-ADR-FULLTEXT`, `OQ-ADR-NUMBERING`).
  `round-of-origin` is mostly `PENDING`. Fixtures 1-14 are likewise RECONSTRUCTED,
  pending ratification (`OQ-FIXTURES-1`).
- **The onboarding test used a Claude reader** — correlated failure. It proves the file
  onboards *a Claude* (the real population), not that the rule is objectively
  unambiguous. Prose-guard efficacy under a more adversarial prompt is unproven; the
  hook (ADR-032) is the real guard.
- **ADR-034 (Sky Model) — my 3 "must resolve before it could be ratified":** (1) a
  pre-registered FALSE-ALARM BUDGET is missing and is the whole survey — whole-sky
  difference imaging is dominated by bogus residuals; (2) `expected` is itself a
  predicted quantity that needs its own calibration — detection relocates the Brier
  problem, doesn't escape it; (3) non-stationarity vs detector-scoring COLLIDE —
  regimes may not last long enough to accumulate the confirmed events needed to grade
  a detector's precision/recall, which threatens the ADR's central promise. Also:
  price+volume are NOT independent confirmation channels; "minimise lead time" is a
  frontier vs false-alarm rate, not a scalar; detector latency ≠ mission lead time;
  and continuous whole-universe 15s difference imaging is not reachable through the
  agent-mediated MCP (its own aperture test fails at that throughput). Added OQs
  proposed: `OQ-FALSE-ALARM-BUDGET`, `OQ-BASELINE-CALIBRATION`, `OQ-RESIDUAL-OVERFIT-GUARD`,
  `OQ-DETECTION-PROVENANCE-LANE`, `OQ-DETECTOR-VS-MISSION-LEADTIME`, `OQ-APERTURE-THROUGHPUT`.

---
*Written at session close for the next builder. Nothing here is ratified that
`OPEN_QUESTIONS.md` and the ADRs don't already say is ratified. When in doubt,
the charter and the gate win, not this log.*
